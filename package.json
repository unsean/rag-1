{
  "name": "rag",
  "private": true,
  "scripts": {
    "dev:inngest": "npx inngest-cli@latest dev -u http://127.0.0.1:8000/api/inngest --no-discovery",
    "dev:api": "uv run uvicorn main:app"
  }
}
